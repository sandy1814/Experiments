pipeline {
    agent any

    stages {
        stage('Docker') {
            steps {
                sh 'docker build -t my-playwright .'
                sh 'docker run --rm my-playwright netlify --version'
            
            }
        }
    }
}